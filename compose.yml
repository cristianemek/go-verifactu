services:
  verifactud:
    build: .
    ports:
      - "9009:9009"
    volumes:
      - ${VERIFACTUD_CONFIG:-/etc/verifactud}:/etc/verifactud:ro
      - verifactud-datos:/var/lib/verifactud
    restart: unless-stopped
    stop_grace_period: 150s

volumes:
  verifactud-datos:
